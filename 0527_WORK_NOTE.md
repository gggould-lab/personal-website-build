# 工作备注：新内容与 Vercel 建立

本次围绕个人网站内容完善、部署准备和 GitHub / Vercel 上线流程排查展开。

已完成内容：

- 更新网站内容结构，补充学术与竞赛、校园经历、实践经历等板块。
- 增加小红书主页跳转链接，并支持文本中使用 `**文字**` 加粗。
- 调整页面顺序，使网页内容顺序与右上角导航一致。
- 将“项目与竞赛”统一调整为“学术与竞赛”。
- 接入简历下载入口，下载文件为 `public/resume.png`。
- 新增 Vercel 部署配置 `vercel.json` 和 `.env.example`。
- 通过生产构建检查，确认项目可以部署到 Vercel。
- 排查本地预览裸 HTML 问题，确认应使用生产构建或 Vercel 线上地址作为正式展示入口。
- 发现旧 Git 历史中存在不规范 tree 对象，已通过干净部署分支方式重新建立可推送的正常提交历史。

后续事项：

- 将干净分支推送到 GitHub 远程仓库。
- 在 Vercel 中导入 GitHub 仓库并完成部署。
- 如需自定义域名，在 Vercel 的 Domains 设置中添加域名并按提示配置 DNS。
